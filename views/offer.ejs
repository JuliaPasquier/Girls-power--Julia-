<%- include('partials/header') %>
    <%- include('partials/nav') %>


        <div class="container">
            <div id="offer">
                <h1 class="title_offer">OFFER</h1>
                <hr>
                <div class="offer-card">
                    <p>
                        <%= offer.jobTitle %>
                    </p>
                    <p>
                        <%= offer.url %>
                    </p>
                    <p>
                        <%= offer.employerName %>
                    </p>
                    <p>
                        <%= offer.employerEmail %>
                    </p>
                    <p>
                        <%= offer.employerPhone %>
                    </p>
                    <p>
                        <%= offer.employerAddress %>
                    </p>
                    <p>
                        <%= offer.offerOrigin %>
                    </p>
                    <p>
                        <%= offer.offerStatus %>
                    </p>
                    <p>
                        <%= offer.comments %>
                    </p></div>
                    <div class="btnUpdate">
                    <a class="btn" href="/update-offer/<%= offer._id %>">UPDATE</a>
                    <a class="btn" href="/delete/<%= offer._id %>">DELETE</a>
                </div>
            
            </div>
        </div>

        <%- include('partials/footer') %>